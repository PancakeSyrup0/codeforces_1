#include <stdio.h>
#include <stdlib.h>
main()
{
    int n,i;
    scanf("%d",&n);
    int p[n],q[n-1];
    for(i=0;i<n;i++){
        scanf("%d",&p[i]);
    }
    for(i=0;i<n-1;i++){
        scanf("%d",&q[i]);
    }

    
}